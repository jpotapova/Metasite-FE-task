import { useTheme } from "@mui/material/styles";
import { type Theme as ThemeProps } from "@mui/material/styles";

import { Theme } from "./Theme";

export { Theme, useTheme };
export { type ThemeProps };
